% Q2.1
% a.
 l = [1;1];
% theta = [any;pi/2;any];
theta = [0;pi/2;0];
figure
plotRobot3D(l,theta)
% Now the elbow is at pi/2
%
% b.
% theta = [pi/2;any;any];
theta = [pi/2;0;0];
figure
plotRobot3D(l,theta)
% Now the whole arm is fully stretched upward in a straight line
% The second one gives a singular Jacobian matrix. Since?
% 